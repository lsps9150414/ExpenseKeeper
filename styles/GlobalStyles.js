'use strict';

import React, {
  StyleSheet
} from 'react-native';

export var GlobalStyle = StyleSheet.create({
  sceneUnderNavBar: {
    position: "absolute",
    top: 56,
    bottom: 0,
    left: 0,
    right: 0,
    // borderWidth: 5, borderColor: '#aaffaa'
  },
})
